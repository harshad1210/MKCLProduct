{
  "name": "MKCLProduct",
  "version": "1.7.4",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "npx prisma generate --schema=prisma/schema.local.prisma && nuxt dev",
    "db:push:local": "npx prisma db push --schema=prisma/schema.local.prisma",
    "db:push:prod": "npx prisma db push --schema=prisma/schema.prisma",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "npx prisma generate && nuxt prepare"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "@vercel/blob": "^2.0.0",
    "date-fns": "^4.1.0",
    "dotenv": "^17.2.3",
    "lucide-vue-next": "^0.562.0",
    "nuxt": "^4.2.2",
    "vue": "^3.5.26",
    "vue-router": "^4.6.4"
  },
  "devDependencies": {
    "prisma": "^5.22.0"
  }
}